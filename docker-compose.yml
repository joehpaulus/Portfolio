services:
  web:
    build: .
    ports:
      - "5173:5173"
    volumes:
      # Mount source so edits on host reflect immediately in the container
      - .:/app
      # Prevent overwriting node_modules from host
      - /app/node_modules
    environment:
      - NODE_ENV=development
